version: "3"
services:
  obsync:
    build:
      context: .
      args:
        GITEA_TOKEN: $GITEA_TOKEN
    image: ghcr.io/mingcheng/obsync:latest
    environment:
      INTERVAL: 12
    volumes:
      - ./config.json:/etc/obsync.json:ro
      - ./data:/data:ro
    restart: unless-stopped
